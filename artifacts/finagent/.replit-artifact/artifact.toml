kind = "web"
previewPath = "/"
title = "FinAgent"
version = "1.0.0"
id = "artifacts/finagent"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22731

[services.development]
run = "pnpm --filter @workspace/finagent run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/finagent", "run", "build" ]
publicDir = "artifacts/finagent/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22731"
BASE_PATH = "/"
